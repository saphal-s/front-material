import { Container, Typography } from '@material-ui/core'
import {moduleName} from '@material-ui/core/colors'
import React from 'react'

function Usecolor() {
    return (
        <div>
           <Container>
               <Typography variant="h3">
                   This is header
               </Typography>
           </Container> 
        </div>
    )
}

export default Usecolor
